Markdown Convert — quick install
================================

Chrome / Edge / Brave
---------------------
1. Unzip this archive.
2. Open chrome://extensions → enable Developer mode.
3. Load unpacked → select the "extension" folder (contains manifest.json).

Firefox
-------
IMPORTANT: Use about:debugging — NOT about:addons.
Installing via about:addons (or double-clicking .xpi) shows "appears to be corrupt"
on release Firefox. Unsigned extensions must load as temporary add-ons.

1. Unzip this archive.
2. Open about:debugging → This Firefox → Load Temporary Add-on.
3. Set file picker to "All Files", then select extension/manifest.json
   (recommended). Do not use about:addons → Install from file.

Note: Firefox temporary add-ons are removed when you fully quit the browser.

Full guide: https://github.com/tanveerriaz/markdown-ext/blob/main/docs/SETUP.md
